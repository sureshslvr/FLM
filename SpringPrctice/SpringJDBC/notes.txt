@Component used when it was user defined class
@Bean used when already defined class like JdbcTemplate

JdbcTemplate need DataSource and it need url username password
JdbcTemplate is coming from springJdbc dependency

JdbcTemplate also handle checked exception and convert it to unchecked exception
	called DataAccessException

spring-context
mysql-connector-j
spring-jdbc

template.update();

for dml we use update()
when using select operation we use rowmapper interface
rowmapper is functional interface
template.queryForObject("",new RowMapper<Employww>(){
							@Override
							public Employee mapRow(ResultSet rs,int rowNum) throws SQLException{
								return new Employee(rs.getInt(1),rs.getString(2),rs.getDuble(3));
							}
						})

it will run for while loop automatically and expect the Pojo class

query() used to get list of objects

DriverManagerDataSource
