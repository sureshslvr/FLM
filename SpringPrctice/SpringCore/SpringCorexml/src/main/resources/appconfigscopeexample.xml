<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"

       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
          http://www.springframework.org/schema/context
          http://www.springframework.org/schema/context/spring-context-3.2.xsd
">
    <!--to enable application properties and read the data properties file-->
    <context:property-placeholder location="classpath:application.properties" />



    <!-- bean definitions-->
    <!--by default scope is single ton in the spring if we use application context -->
    <!--
    singleton: Default scope.,Only one instance per Spring container,Shared across the application context.
    prototype: A new instance is created every time the bean is requested.
    request: One instance per HTTP request,Used in web-aware contexts.
    session: One instance per HTTP session.
    application: One instance per ServletContext.
    websocket: One instance per WebSocket session.
    -->
    <bean id="emp" class="org.com.Employee" scope="prototype">
        <property name="id" value="2"/>
        <property name="name" value="suresh" />
        <property name="address" ref="address"></property>
    </bean>
    <bean id="address" class="org.com.Address">
        <property name="address" value="${address}"/>
    </bean>
</beans>
